CREATE OR REPLACE TRANSIENT TABLE ANGEL_CITY_HEALTH_DB.DEV.FCT_CLAIMS

AS (

    SELECT
        CLAIM_ID
        , ENCOUNTER_ID
        , PAYER_ID
        , PROCESSED_DATE
        , TOTAL_BILLED_AMOUNT
        , AMOUNT_PAID_BY_INSURANCE
        , PATIENT_RESPONSIBILITY_AMOUNT
        , REIMBURSEMENT_RATE
        , CLAIM_STATUS
        , DENIAL_REASON
        , LOAD_TIMESTAMP
    FROM ANGEL_CITY_HEALTH_DB.DEV.STG_CLAIMS
);
